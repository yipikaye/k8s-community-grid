To get logs for all Pods
kubectl logs -f -l app={{ include "k8s-community-grid.name" . }} --all-containers -n  {{ .Release.Namespace }}


kubectl scale deployment {{ include "k8s-community-grid.fullname" . }} -n  {{ .Release.Namespace }} --replicas=X
where X replicas = number of worker nodes or Pods you want to run